name: Build & Test Solana Contract

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - 'contracts/solana/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    container:
      image: solanalabs/solana:v1.18.0
    steps:
      - uses: actions/checkout@v4

      - name: Remove Cargo.lock
        run: |
          echo "=== Removing Cargo.lock from checked out code ==="
          find . -name "Cargo.lock" -type f -delete
          echo "✓ Cargo.lock removed"

      - name: Install Dependencies
        run: |
          echo "=== Installing dependencies ==="
          apt-get update
          apt-get install -y curl build-essential pkg-config
          echo "✓ Dependencies installed"

      - name: Install Rust
        shell: bash
        run: |
          echo "=== Installing Rust ==="
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | bash -s -- -y --no-modify-path
          export PATH="$HOME/.cargo/bin:$PATH"
          rustup install nightly
          echo "✓ Rust and nightly installed"

      - name: Prepare Build
        shell: bash
        working-directory: ./contracts/solana
        run: |
          export PATH="$HOME/.cargo/bin:$PATH"
          echo "=== Remove lock file to avoid v4 issues ==="
          rm -f Cargo.lock
          ls -la Cargo.lock || echo "Lock file removed"

      - name: Build Solana Contract
        shell: bash
        working-directory: ./contracts/solana/programs/forge_solana
        run: |
          # Setup Rust environment
          export PATH="$HOME/.cargo/bin:$PATH"
          
          echo "=== Checking cargo ==="
          which cargo && cargo --version
          
          echo "=== Current directory ==="
          pwd
          ls -la
          
          echo "=== Starting build with cargo build ==="
          cargo build --release --target bpf || cargo build --release

      - name: Verify Build Output
        working-directory: ./contracts/solana/programs/forge_solana
        run: |
          echo "=== Checking build output ==="
          ls -lah target/bpf-solana-solana/release/ || echo "Deploy directory not found"
          if [ -f target/bpf-solana-solana/release/forge_solana.so ]; then
            echo "✓ Build successful!"
            file target/bpf-solana-solana/release/forge_solana.so
          else
            echo "✗ Build output not found"
            find . -name "*.so" -type f 2>/dev/null || echo "No .so files found"
          fi

      - name: Upload Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: solana-build-artifacts
          path: contracts/solana/programs/forge_solana/target/bpf-solana-solana/release/forge_solana.so
